services:
  - type: web
    name: microfinance-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: FB_PAGE_ACCESS_TOKEN
        value: YOUR_FACEBOOK_PAGE_ACCESS_TOKEN_HERE
    healthCheckPath: /
    autoDeploy: true 